import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  Title: {
    color: "white",
    fontWeight: "bold",
    fontSize: 24,
    textAlign: "center",
    borderWidth: 2,
    borderColor: "white",
    padding: 12,
  },
});

export default styles;
